# 服务器现状检查

检查时间：2026-09-10 22:19～22:20（Asia/Shanghai）。用户提供 `10.160.108.2` 并授权使用本机已有 SSH 密钥查看 Docker、Ollama 和模型。本次只读检查，没有启动、停止或修改远程服务。

这份文档保留首次只读检查时的历史快照。用户之后已授权整个后端开发，并说明服务器没有外网；当前部署及真实推理状态见 [后端实现与部署验收](后端实现与部署验收.md)。以下“尚未实现”“没有启动”仅描述首次检查。

## 连接与硬件

| 项目 | 实测结果 |
| --- | --- |
| SSH | 本机已有配置可免密连接；用户 `user2`，端口 22；已校验现有主机密钥 |
| 系统 | Ubuntu 22.04.5 LTS，Linux 6.8.0-124-generic，x86_64 |
| GPU | 2 × NVIDIA GeForce RTX 4090，每张 24564 MiB 显存 |
| 检查时 GPU 占用 | GPU 0：41 MiB；GPU 1：15 MiB；两卡利用率均为 0% |
| NVIDIA 驱动 | 580.173.02 |
| 内存 | 总计 125 GiB，约 120 GiB available |
| 磁盘 | 根分区约 39 GiB 可用；`/data` 约 319.7 GiB 可用 |
| Docker | Engine 29.1.5；数据目录 `/data/docker_root`；当前用户可执行 Docker 只读查询 |
| Python | 主机现有 Python 3.10.12；项目此前规划的 3.12 尚未安装或验证 |

占用和可用空间是检查时快照，部署前应再次核对。未读取私钥正文、应用环境变量或业务日志。

## Ollama 容器

- 容器名：`ollama`；配置镜像：`ollama/ollama:latest`。镜像标签不是实际 Ollama 版本，本轮未运行版本查询。
- 状态：`exited`，退出码 0；最后停止时间 `2026-05-31T01:55:34Z`。
- 数据挂载：主机 `/data/ollama_data` → 容器 `/root/.ollama`。
- 端口配置：宿主机 11434 → 容器 11434/tcp，未指定宿主绑定 IP；检查时没有 11434 监听。
- GPU 配置：请求全部 GPU。
- 重启策略：`no`。

还存在另一项目的已停止 Docker 容器，本次未操作。

## 已下载的模型

读取 `/data/ollama_data/models/manifests` 及对应的小型模型配置文件，得到两个模型标签：

| 模型标签 | 元数据中的参数量 | 格式/量化 | 文件总量 |
| --- | --- | --- | --- |
| `qwen3.5:9b` | 9.7B | GGUF / Q4_K_M | 约 6.14 GiB |
| `gemma4:31b` | 31.3B | GGUF / Q4_K_M | 约 18.50 GiB |

因此当前清单中的千问是 **`qwen3.5:9b`**，没有发现 7B 标签。每份清单引用的配置、权重、许可和参数文件均存在，文件大小与清单一致。本轮未读取全部权重计算哈希，也没有启动模型做推理；文件存在及大小正确不等于推理兼容性已验证。

## 对后续后端的影响

现有 Ollama 容器及模型可作为后续集成起点。恢复服务后需核对实际 Ollama 版本，使用合成输入验证 `/api/tags`、基本生成和结构化输出，再接入 FastAPI。无需在开始后端开发时先选择或下载另一套模型。

本次没有把浏览器插件连接到服务器，FastAPI 后端也仍未实现。后续如使用 Python 3.12，应放在项目独立环境或容器中，避免直接替换服务器现有 Python。
