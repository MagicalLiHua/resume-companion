# 前序对话交接

记录日期：2026-09-10。

## 来源

- 原任务标题：**下载插件源代码**。
- 原任务 ID：`01a08b2a-286d-7432-875a-8b45de5370a3`。
- 原任务没有关联项目，研究输出保存在当时的本地工作目录。
- 当前仓库为独立实现，不包含原扩展发布文件。

## 用户已表达的要求

1. 研究本机安装的“求职方舟”Chrome 插件，理解其源代码和工作方式。
2. 希望使用自己部署的模型或服务，保留浏览器插件形态。
3. 使用者为自己和几个同学，不以商业平台为第一阶段目标。
4. 有一台双 RTX 4090 服务器，后期可运行模型和后端。
5. 用户不熟悉后端，由助手选择适合且便于兼容的后端语言。
6. **先写详细、可行的完整开发计划，由用户审阅后再开发；服务器后面再说。**
7. 用户在当前任务中要求继续此前工作；网络中断后又明确要求继续。完成计划与复用评估后，用户确认“ok的呐 那现在是不是可以开始实施了”，已授权进入实施，不需要再次请求开始开发。
8. 用户随后提供服务器 `10.160.108.2`、说明本机已配置 SSH 密钥，先授权检查，再明确要求“帮我完成整个后端开发”。服务器与后端不再处于暂缓状态。
9. 当前补充约束：服务器没有外部网络连接。依赖与基础镜像在本机准备，经过校验后通过 SSH 传入，服务器离线构建和运行。
10. 用户在后端开发中追加下一阶段方向：网页端简历维护与 PDF 解析、邀请码注册、用户创建 API Key 后填入插件、插件同步并自动填写。允许服务端保存账号下的已确认简历；替代早期仅本地保存和暂缓 PDF 的范围，见 [网页端与账号体系方案](网页端与账号体系方案.md)。
11. 用户明确认为无需继续在当前小模型上花时间；停止效果调优，保留已有评测，后续可换模型或用 vLLM。基础后端收尾只做必要连通与交付检查。

## 已完成的前序工作

已从 Chrome 安装目录提取扩展发布包，版本 `2026.9.9`，并完成静态分析。这里的“源码副本”是浏览器运行用的发布文件，不代表取得完整开发工程或服务端实现。

- 前序静态审计报告（本地研究材料，未纳入本仓库）
- 原发布包压缩文件（本地研究材料，未纳入本仓库）
- 原扩展权限清单（本地研究材料，未纳入本仓库）
- 原发布包附带的旧版可读脚本（本地研究材料，未纳入本仓库）

前序检查发现原扩展存在较广的页面注入和内容上传行为。新项目采用主动启动、结构化字段提取、本地数据优先的设计。原文件留在原目录，作为研究材料；当前项目自行实现，不把压缩后的商业发布代码作为工程基础。

## 本次规划结论

- 插件：Chrome Manifest V3 + TypeScript，React 侧边栏，Vite 构建。
- 业务后端：Python 3.12 + FastAPI + Pydantic。
- 模型：先模拟适配器，后期接私有推理服务；兼容接口不代表调用外部付费服务。
- 用户简历保存在浏览器本地；服务端无需简历数据库。
- 先完成规则填写闭环；模型辅助、站点适配、同学试用按里程碑推进。

前序对话给出的具体模型和双卡命令属于候选方案。计划保留 `Qwen/Qwen3.8-27B-FP8` 为后期评测候选，但不承诺它在这台尚未检测的服务器上可按原命令直接运行。显存分配、推理框架、双卡通信、上下文长度和并发需要实测。

## 当前状态与下一步

用户后续审阅反馈：优先分析是否能利用原插件已有的多网站适配能力；第 2 项导入功能暂缓（当前按 Word/PDF 自动导入理解）；接受先做填写、AI 增强后置。已追加《原插件适配能力与复用评估》，确认 3 类平台的 5 个路由分支和原厂后端依赖。首轮两个真实流程改为验收样本，不视为产品支持上限。随后用户明确确认开始实施。

当前已创建独立实现的 Manifest V3 + TypeScript + React 工程，完成本地资料编辑、扫描、来源绑定、预览、填写、回读与撤销。依赖与构建已完成，23 项数据/规则测试和 32 项浏览器测试通过。可加载构建在 `dist`，安装包在 `artifacts/resume-companion-0.1.0.zip`。详情见 [本地版实现与验证](本地版实现与验证.md)。

首次只读服务器快照见 [服务器现状检查](服务器现状检查.md)。后续按新授权实现了 FastAPI 后端、认证与令牌管理、请求和模型结果校验、限流、取消与总超时、模拟及真实 Ollama 适配、启动预热、接口契约与离线部署。服务位于服务器 `/home/user2/resume-companion-api`，API 仅绑定回环端口 `18080`，模型只用 GPU 0；原有停止中的 Ollama 容器保留。实际运行的是现有 `qwen3.5:9b`，没有下载另一个模型。

源码与使用方式见 [后端说明](../backend/README.md)，部署、真实评测和未测边界见 [后端实现与部署验收](后端实现与部署验收.md)。个人令牌只保存在本机 `backend/.runtime`，不写入交接文档或发布包。当前仍未在插件中接入 AI 设置、调用按钮和草稿预览；这些属于后续前端联调。

没有发布到商店或提交求职申请。真实招聘网站尚未适配和验收；后续优先实施网页账号与简历维护、PDF 导入和插件 API Key 同步，再推进代表性网站适配。Word 导入仍后置。
