# 更新记录

## 0.24.0 · 本地自动规划与简历多样性（开发预览）

- 项目对外名称更新为 ApplyMCP，并重写 GitHub README，按产品定位、完整用户流程、支持范围、安装、隐私和人工边界组织首屏信息；新增裁剪、加速的 GIF 演示及高清 MP4。各客户端插件 ID 与显示名称统一为 `applymcp` / ApplyMCP；MCP 服务名、仓库路径和本地数据目录保持兼容。
- 新增多 Agent 兼容层：Claude Code、Cursor、Kimi Code CLI 与 Agent Plugins v1 原生清单，WorkBuddy 双连接器发布包，以及 Trae、OpenCode、Gemini CLI 和通用 stdio MCP 配置生成器。兼容测试核对版本、路径、双服务和包内依赖；招聘系统逻辑与 Skill 仍只维护一份。
- 新增招聘平台兼容扫描：保留精确企业模板证据，同时在可信来源和结构家族同时匹配时支持同系统其他企业页面。未知可选字段/模块跳过并提示，未知必填内容在写入前阻断；51job 继续精确企业模板门控。
- `form_prepare` 新增 `module_selection` 和 `profile_only_sections`，直接展示页面开放模块与用户 provided / none / unknown 资料的交集、计划记录和步骤数。Moka、北森、大易、国聘开放平台兼容扫描；飞书当前可信来源限已验证招聘站。

- 2026-09-21 适配修复：宿主 Agent 解析结果入库指导；修复后台指针操作等待和虚拟列表滚动渲染竞争。一汽去重字段别名、区分培养方式语义；国聘新增单独结束日期识别、项目预览绑定及自我评价映射。
- 国聘自我评价与三条项目已保存刷新核对；一汽部分普通栏目已有刷新证据。两站仍为开发中，不开放完整自动填写能力声明。

- 资料准备更新：新增 `resume_prepare` 和 `resume_prepare_apply`，按已验证模板映射需求生成一次性缺项 Markdown；按记录 ID、revision、目录版本和问卷 ID 合并答案，不覆盖已有事实。
- 手机号/邮箱已有则直接复用；身份证等证件号码单列本地录入项，当前尚未实现私密入口。明确没有、不适用、暂不提供、稍后补充分开存储；不会转成网页“否”。在读与已获学位分别处理，高中不问学位。
- 当前表覆盖已映射需求；开发中模板、未映射字段、私密引用仍不在本批交付范围；PDF/Word 解析由宿主 Agent 负责。

- 2026-09-21 收敛更新：增加企业模板支持检查；企业亲属、声明、凭据与附件统一人工处理。单页先完成普通字段，分页只在必需人工门槛处暂停，恢复先核对普通字段。
- 原生单选题按整组判断已回答；附件区分选择文件和可见上传完成。默认后台创建/选页，人工处理、错误和完成提醒按事件去重；原生窗口焦点仍待桌面验收。
- 整页与分页执行默认返回摘要、计数和异常；明细按需分页，状态变化拒绝混读。修复恢复成功后残留旧错误，保护字段的最终未变核验独立报告。保存持久化仍单独验证。
- 当前不宣称全部同平台企业都经过原站验收；一汽大易和国聘普通模块已有样本证据，江苏银行仍为开发中。一次性补充表已在后续更新交付；私密引用尚未交付。

- 补齐中粮 51job 十个普通页面的字段规则：最高学历条件展开、按学历匹配固定块、跨列“其他”学校/专业、竞赛、获奖等级、合并证书和父母关系映射。实际原站与自动复跑结果见开发记录。
- 照片边界仅在已加载的网页预览可确认时解除；附件由用户上传，正常工具入口不代上传。修复虚拟列表从残留滚动位置开始而漏选的问题。
- 新增 51job 分页 `form_journey`、原生联动、固定学历位置、重复记录和 My97 日期；每页核验后才进入普通下一步，最终提交及人工声明停止。
- 新增大易 Ant Design 3 控件与科研/家庭资料映射，支持首次添加产生两名家属的模板、完整日期精度和中断后部分记录恢复。
- 点击前检查遮挡和节点稳定性；新记录不继承旧卡片的值。原站验收边界详见 docs/51job与大易适配开发记录-2026-09-21.md。

- 新增只读 `form_prepare`：从指定资料版本和实时完整页面生成计划，返回短期 ID 与可分页差异，减少模型搬运事实和手写动作。
- schema 1.2 兼容读取 1.1，补充求职意向、语言、获奖、竞赛、校园经历及三态栏目状态。未提供、明确没有、有记录分别处理。
- 飞书 UD、Moka SD、北森 Phoenix 规则以当前企业页面模块为准；不生成缺失栏目，不新增空白可选经历，支持少量有来源的自定义字段绑定。
- 准备结果绑定任务、页面和资料版本，10 分钟过期；写入前核对页面，记录边界核对资料 revision。保留旧手工计划接口和执行生命周期。
- 结果区分字段覆盖、源资料无对应字段、冲突和未知能力；不声称已保存或适配同系统所有企业。

## 0.23.0 · Moka 与北森 Phoenix（开发预览）

- 北森 Phoenix 新增栏目/重复记录、性别单选、普通候选、日期日/月选择、城市与行业确认多选、职业目录和学校词库事务；学校查询不再误判为已提交值。
- 选择器按稳定容器跟踪，提交后 input 消失仍可读取标签；候选确认后读取新节点，关闭时等待旧 portal 退场。
- 修复受控搜索框聚焦恢复旧值后拼接查询；Moka 打开选择控件使用真正的触发器。
- 新增原创 Phoenix React 整页与失败反例回归，并纳入 CI；技能补充北森整页计划和词库限制。
- 原站验收与缺项见 docs/validation.md；未测试服务端保存与刷新持久性。

## 0.22.0 · 飞书事务收尾与 Moka SD（开发预览）

- 专用浏览器发现端点和启动锁不再依赖 TMPDIR；复用旧端点，升级默认保留未保存页面。重载脚本须带 `--restart-browser` 才关闭旧服务。
- 飞书 reveal 可绑定唯一新增卡片；关闭浮层幂等且校验未改值，失败顶层 generation 与 error 一致，标题过滤样式噪声。
- Moka SD 新增重复卡片范围、基础身份标签、证件类型区分和年月生日事务；年龄后缀不参与日期比较。兼容没有 common-item 包装的年月菜单、选年自动补月和清除占位符后的日期组/子字段身份。
- 学校/专业搜索增加明确 allow_custom 授权的控件内自定义名称事务；整页计划支持 query_from_value，缺候选不视作已填。
- 整页多选追加核验原有选项与新值的并集；技能补充一次性覆盖决策、初始空卡片复用和完整计划范例。
- 原站结构已核对；本版本整页原站写入及保存持久性仍待验收。本地组件和整页测试不能替代原站结果。

## 0.21.0 · 整页计划与可靠覆盖（开发预览）

- 识别前置开关自动生成的经历记录，并交给依赖计划接管，避免额外新增空壳。
- 新增 `form_run`，一次传入事实、记录绑定、依赖及保护字段，内部连续执行并最终回读；页面必填缺项、错误和未覆盖字段单独列出。
- 90 秒前台窗口、记录检查点、180 秒总预算；查询和取消不等待浏览器锁。连接关闭和租约接管停止后续派发，显式恢复不重放成功项。可先登记计划取得恢复凭据。
- 修复 React 聚焦恢复原值导致的文本拼接；短文本、邮箱、电话、数字和长文本统一确认选区后输入。修复 fields 顶层 scope 继承。
- 日期复用已打开的端点并支持年份面板核验；添加前唯一重绑定旧按钮，记录重挂载仅在完整原状态唯一匹配时重绑定。
- 观察展示与业务状态比较分离；自由名称能力、编辑/提交状态和损坏敏感值遮罩补齐。
- 新增真实 MCP 生命周期、完整虚拟简历空白/已有值流程及相关反例回归；本地 React 夹具不等于原站组件，保存持久性仍需单独验证。

## 0.20.1 · 浏览器执行上下文恢复（开发预览）

- 表单操作和原生输入之前检测主世界及工具世界；上下文缺失时在原 CDP 会话重新同步，不刷新页面、不重放输入。
- 修正上游将执行上下文超时包装为失效 UID 后触发语义歧义的问题。无法恢复时返回 `page_context_unavailable` 并保留未保存页面。
- 扫描代码异常与真实超时分开报告；取消 beforeunload 后返回 `navigation_cancelled`，不再被识别为刷新成功。
- 新增只读 `npm run debug:browser -- runtime <pageId>`，仅输出各 frame 上下文是否存在。

## 0.20.0 · 飞书可靠输入与记录连续执行（开发预览）

- 文本输入使用浏览器原生文本插入，修复底层长文本直接赋值绕过 React 状态的问题；失焦后和普通批次结束时重新核对，不把瞬时 DOM 值当作完成。
- 识别已验证 UD 自由输入结构、Formily 字段错误，以及带相邻文字的 UD 树复选多选；错误保留记录归属，候选失败返回局部证据。
- 新增前检查滚动、稳定与命中；以目标栏目新增一条记录验证成功，返回新记录字段摘要，遮挡与不确定结果不会自动重试新增。
- `form_observe` 提供可续读的字段目录；测试日志默认摘要、详情分页，保留运行 ID、累计计数及最近 5,000 条明细。显式详情请求使用 `ledger_limit`，不再默认附加最后 100 条记录。
- 保留六个表单工具，`form_fill_fields` 新增单记录 `steps` 模式，顺序复用文本、选择和日期执行器；支持取消、记录身份检查、幂等重试、失败停止及末尾回读。
- 新增长文本 React 反例、整批执行/取消、UD 多选、错误归属、遮挡、新增和分页回归。完整原站 Agent 耗时及服务端保存需另行验收，尚不宣称五分钟或两分钟达标。

## 0.19.0 · UD 教育经历语义填写（开发预览）

- 读取 UD/Formily 的真实标签、必填提示与模块/记录归属；同名字段按记录容器区分，不依赖原站重复的 HTML ID。
- UD 固定单选接入现有选择工具；区分只包普通文本输入的 UD 外壳，避免把学校文本错误回读为空值。
- 起止年月作为日期组由一次 `form_set_date` 处理，分别重开两端面板核对已选月份，并保留已有值、精度检查和取消预算。
- 增加完整教育经历的语义工具验收，以及真实 React 运行时的回滚、重绘、重复候选、禁用月份和 DOM 值伪装反例。React 行为夹具使用观察到的 UD 结构，不是原站 UD 组件实现。
- 飞书招聘原站完整教育经历验收通过；修复 UD 候选已选中但弹层尚在收起时被误报失败的问题，等待弹层关闭后重新验证，并增加动画期间值回滚的反例。

## 0.18.0 · 日期组与 SD 选择流程（开发预览）

- 将有明确标题和年/月结构的 SD 子选择器作为 `date_group` 返回。Agent 可一次 `form_set_date` 完成单个月份或起止年月，工具内部等待联动、处理“至今”、保护已有值并核对整组结果。
- 日期子选项支持 `6`、`06月` 等明确数字格式；等价候选不唯一时停止，普通下拉仍保持严格文字匹配。
- 多列 SD 菜单接入现有 `form_select_path`，候选限定到本控件弹层，等待子层更新；必须核对完整提交路径，只有末级回显或错误父级不算成功。标签页换层、无完整路径回显等变体仍需进一步验证。
- Agent 先批量完成无未满足依赖的简单字段，再处理复杂控件；局部失败不会要求重填整个批次。保持原有六个语义工具。
- 新增隔离浏览器回归，覆盖记录隔离、日期组联动、同值幂等、已有值、至今、数字歧义、值回滚及 SD 多列路径。真实招聘网站完整填写仍待监督验收。

- 根据 Moka 页面公开 DOM 证据补充 `sd-Select` / `sd-Menu` 结构族的单选、候选滚动和已选展示值读取；不依赖域名、框架私有状态或 CSS 哈希后缀。
- 对缺少 ARIA 标签的组件，使用相邻标题与控件容器恢复字段名及栏目，避免将选中值、占位符或校验文字当作字段名；手机号与区号分开定位。
- 保留单个年月子控件的逻辑引用，同时提供日期组入口；该结构族的多选、标签页地址及其他日期面板未包含在本轮新增支持范围。
- 新增原创合成回归，覆盖重复字段作用域、已填值保护、无角色候选、搜索值与选中值区分、重复/禁用候选及年月子控件。真实页面只做结构对照，未选值、保存或提交。

## 0.17.0 · 复杂控件执行与结果验证

- 新增真实 Ant 4/5、Element Plus 组件测试与受控值校验，保留 0.16.2 基线包。
- 日期支持原生日期/月份、公开格式明确的输入提交、只读日历、年月切换与确认；等待弹层退出并用只切换视图的导航复核选中状态，避免把临时日期预览当成提交值。
- 级联验证完整父路径与候选更新，支持点击/悬停、异步子层、父级选择和仅末级回显；不再只凭叶子名称宣称完成。
- 支持候选容器有界滚动、多选 Select 的增补/显式替换、组合日期范围和分开日期控件的至今联动。无法证明完整状态的折叠多选明确停止。
- 所有新增控件动作共享有界等待、取消、动作派发记录和作用域；候选在派发前再次核对语义，未派发事件的节点失效允许重新定位一次。
- 收紧字符串/数值匹配和已有值保护；区分界面验证与持久化，保存点击不再被描述为已保存。
- 操作 ID 绑定参数和文档，导航保持 generation 单调；相同 ID 的不同请求与过期缓存不再静默复用或重放。
- 更新 MCP schema、Agent skill 和 CI 的真实组件检查，保留人工上传、声明与最终提交边界。

## 0.16.2 · 自定义选择器与逻辑引用修复

- 统一观察、候选解析、元素定位和原子回退使用的 DOM 语义；支持 Ant Select 的无 role 可见候选、listbox 文本节点及 Cascader 的 menuitemcheckbox。
- 候选按自身文本读取并限定到所属弹层；已经打开的弹层不再次点击触发器，级联按实时列结构处理不同深度路径。
- Select/Cascader 从展示节点读取已选值，搜索 input 的空值或临时查询文本不再充当选择状态。
- 逻辑引用保留元素身份与 frame，scope 作为过滤条件，page 表示全页；仅定位失败的激活动作不推进 generation，也不报告动作已开始。
- 使用页面导航锚点识别栏目，避免把栏目内记录卡片当作栏目；普通导航菜单不再作为候选弹层。
- focus 支持裸标签和完整引用；未找到目标时明确返回 target_unresolved。扫描共享标题索引，执行上下文失效后对当前 frame 局部重试。
- evaluate_script.args 的实际工具 schema 明确仅接受快照元素 UID，普通字面量应放在函数体中。
- 新增贴近真实组件结构的合成回归及 SPA 扫描重试测试；保留上传、声明和最终提交的人工边界。

## 0.16.1 · Supervisor 冷启动与桌面更新修复

- 使用按 Profile 派生的原子启动锁串行化冷启动；多个任务同时加载插件时只会创建一个 Browser Supervisor。
- 安装更新脚本会先终止旧 Supervisor 并撤销它的浏览器会话；App Server 控制端点可用时继续热重载 MCP，Codex 桌面版未开放端点时明确提示新建任务，无需重启应用。
- 发布包冒烟改为两个 MCP 客户端同时冷启动，验证它们最终共享同一个 Chrome、租约和页面状态。
- 原始 `fill` 返回成功后核对当前语义节点；SPA 在动作结束瞬间替换节点并丢失值时，等待目标稳定后只补写一次。

## 0.16.0 · 跨任务浏览器接管与 MCP 热重载

- 新增独立 Browser Supervisor。多个 Codex 任务连接到同一个专用 Chrome、持久 Profile、标签页和语义缓存，不再为每个任务启动浏览器或争抢 Profile 锁。
- 新任务在第一次浏览器调用的原子边界自动接管操作租约；旧任务保持打开并收到明确的 `browser_lease_revoked`，不会继续并发操作页面。
- 新增 `browser_takeover`，允许用户明确要求旧任务重新取得控制权；当前所有者随即被撤销。
- Supervisor 使用仅当前系统用户可访问的 Unix socket 或 Named Pipe，按 Profile 隔离，并在新版客户端连接时执行协议与版本升级。
- 冷启动使用按 Profile 派生的原子启动锁，多个任务同时加载时只会创建一个 Supervisor。
- 新增 Codex App Server MCP 更新脚本。控制端点可用时刷新已加载任务；桌面版未开放端点时通过新任务加载，无需重启整个 Codex 应用。
- 新增双 MCP 客户端集成回归，验证共享同一 Chrome、自动接管、旧任务隔离和显式夺回。

## 0.15.1 · 真实 SPA 事务恢复与只读实时调试

- 语义候选必须同时匹配目标文字和控件角色，字段触发器与弹层候选分开解析；修复下拉或级联已打开时把同名候选算作字段，以及无文字命中时误点其他控件的问题。
- 下拉和级联事务覆盖“动作报错但页面已经变化”的情况：先核对已选值或下一层候选，再决定成功、继续或返回带弹层状态的 `action_result_unknown`，避免重复打开和重放副作用。
- 每个可能写入页面的事务返回最新 generation；旧 generation 明确返回 `generation_conflict`。临近表单的可见标题可识别为 section。
- `form_observe` 对各 frame 设置独立时间预算并标明超时阶段；低层超时附带定位、事件确认或截图阶段和一次性恢复建议。
- 原始快照及携带快照的低层结果默认遮蔽手机号、身份证、邮箱、银行卡和出生日期；低层回退可携带 `test_mode` 与 `operation_id` 并记为未验证清理项。
- 新增由浏览器所有者进程提供的本机只读调试 socket。开发者可实时列出标签页并执行脱敏局部观察，无需启动第二个 Chrome 或争抢专用 Profile 锁。
- 新增真实反馈合成回归，覆盖标题 section、短命 readonly combobox、20 个级联候选、动作副作用、generation 冲突、原始快照脱敏与调试桥接。

## 0.15.0 · Resume Browser MCP

- 用同进程 TypeScript 组合服务替代上游子进程转发：继续复用固定的 Chrome DevTools MCP 1.9.0 浏览器生命周期和诊断工具，同时共享页面上下文、互斥锁与专用 Profile。
- 新增 `form_observe`，支持 overview、focus、delta 和显式 full；观察结果带 generation、逻辑字段引用、响应预算、裁剪信息和敏感值脱敏。
- 新增 `form_fill_fields`、`form_select_option`、`form_select_path`、`form_set_date` 与 `form_activate`，在浏览器进程内完成动态定位、输入、等待、回读和局部结果返回。
- 事务动作支持 generation 冲突、幂等 operation ID、已有值保护、约束预检、局部错误分类和测试操作清单；明显的提交、声明、上传与不可逆边界由 `form_activate` 阻止。
- 删除编译后上游字符串补丁和独立预加载产物；原始 DevTools 工具继续作为诊断和显式回退。
- `resume-autofill` 默认使用局部观察和事务工具，完整快照与 UID 循环不再是日常流程。
- 新增长页面预算、隐私脱敏、三级级联单调用、日期、增量、幂等、人工边界、持续 DOM 重建与 Profile 重启持久性回归。

## 0.14.0 · 动作期间节点替换恢复

- 将 SPA 恢复范围从“取得句柄前”扩展到 Locator 的完整 `fill`、`fill_form`、`click` 和 `hover` 动作周期。
- 绝对赋值失败后先重新定位并核对目标值；字段已经写入时直接确认，未写入时只重试一次。
- 监听 Locator 的真实动作起点：点击尚未派发时允许一次恢复；点击已经开始后返回 `stale_action_result_unknown`，避免重复添加记录、保存或导航。
- 恢复动作关闭无意义的两帧边框稳定等待，仍保留视口、可用性和唯一语义检查。
- 新增持续重建 DOM 的回归页，覆盖动作期间反复替换输入框与按钮，并验证按钮只触发一次。
- 固定上游运行包保留 `stale_action_*` 原始错误码；版本签名不匹配时构建直接失败，不静默改写未知版本。

## 0.13.0 · 真实 SPA 短命节点恢复

- 为固定的官方 Chrome DevTools MCP 1.9.0 增加 TypeScript 预加载兼容层；官方工具名、协议、Profile 和浏览器连接方式保持不变。
- 动作前检查 UID 句柄是否仍连接到文档；节点被 SPA 替换时，按角色、可访问名称和最近命名分组即时重定位。
- 只有唯一候选或上下文能明确区分时才恢复；重复控件无法区分时返回 `stale_uid_ambiguous`，不按 DOM 顺序或键盘次数猜选。
- 新增高频重渲染合成页与端到端回归：在快照后替换输入框和按钮，原 UID 的 `fill` 与 `click` 仍成功。
- 真实网站捕获目录加入 Git 忽略规则；公开仓库只保留重新设计的合成复现，不分发交通银行页面资源或脱敏快照。

## 0.12.0 · 控件配方与复杂控件实验室

- 新增十五类通用控件配方，按 L0 独立字段、L1 触发字段、L2 动态候选、L3 记录边界和人工边界规划页面。
- 新增独立复杂控件实验室，覆盖自定义下拉、异步搜索、三级级联、树选择、日期至今、弹窗、虚拟分页、重复记录、懒加载、iframe、contenteditable 和自动保存。
- 固定版官方 Chrome DevTools MCP 连续五轮完成全部复杂场景，每轮 32 次工具调用，最终提交和声明边界违规均为 0。
- 新增浏览器生命周期回归：运行中的专用 Chrome 直接复用；窗口退出后原 MCP 在下一次调用重新启动，并保留同一 Profile 的站点状态。
- 新增竞品经验来源目录，只提炼可观察行为和测试场景；商业代码、远端配置、后端和站点选择器不进入产品或发布包。
- 新增可复制的新任务验收提示词、虚构资料和阶段评测记录。
- 本地跨任务经验库继续受评测决策门约束；在五个独立 Agent 对照任务证明收益前，不新增经验工具和站点数据持久化。

## 0.11.1 · 专用 Chrome 延迟锁与综合验收

- 新增在 Resume Companion 专用 Chrome Profile 中运行的四步综合验收页、配套虚构简历和可直接复制的新会话任务。
- 综合页覆盖动态地址、异步学校候选、重复经历、批量复选框、条件字段、草稿请求、已有答案保护和最终提交边界。
- 直接 Chrome DevTools MCP 集成回归增加综合页首步的真实填写与跳转检查。
- Chrome Profile 实例锁延迟到第一次真实 `tools/call`；任务启动、初始化和工具目录读取不再占用浏览器。
- Profile 被其他任务占用时返回可重试错误，不终止 MCP；占用者退出后原任务无需重启即可再次调用。
- 新增双 MCP 客户端实测，覆盖同时加载、第一次调用互斥和释放后的接管。

## 0.11.0 · 资料 MCP + 官方 Chrome DevTools MCP

- Resume Companion MCP 收缩为四个本地资料工具；浏览器能力直接使用固定的官方 Chrome DevTools MCP 1.9.0。
- 新增 TypeScript Chrome 启动器、跨平台持久 Profile、保守的单实例锁和脱敏启动诊断。
- 客户端浏览器工具默认逐次审批，只自动开放审查过的常规观察与填写工具；禁用上传和 Lighthouse。
- `resume_profile_read` 新增服务器端 `expected_revision` 校验，防止一次任务混用不同版本的资料。
- 重写 `resume-autofill` skill，覆盖批量填写、部分成功恢复、已有答案保护、四级保存证据和按缺失证据选择诊断能力。
- 从主线和发布包移除扩展、Native Host 与自研浏览器协议；旧实现保存在 `archive/extension-0.10.0` 标签。
- 新增直接官方 MCP 的文本、下拉、复选框、部分失败、等待快照和持久 Profile 集成回归。

## 0.10.0 · 当前 Chrome Profile 的扩展主驱动

- 默认驱动改为精简 Chrome 扩展，直接复用日常 Profile 的登录、Cookie 和已打开页面。
- 新增 Chrome Native Messaging Host，通过 0600 描述符、短期随机 token 和私有 Unix socket / Named Pipe 连接 MCP，不开放固定 localhost 端口。
- 恢复并重构为 TypeScript 的通用页面引擎，覆盖批量写入、搜索下拉、级联、日期、虚拟列表、快照、回读、去重和条件撤销。
- 需要可信用户输入时仅对目标标签页按需使用 `chrome.debugger`；暂停或断线后分离。
- 扩展界面只保留连接状态、重连和暂停，不包含简历、模型配置、悬浮球或投递记录。
- 官方 Chrome DevTools MCP 1.9.0 保留为显式备用和 CI 驱动，不再承担日常 Profile 连接。
- 在 Chrome 153 当前 Profile 实测列出、激活并观察交通银行简历页，不需要远程调试或新 Profile。
- 发布包加入扩展、Native Host、安装器与完整公开文档，生成的 JavaScript 在 GitHub Linguist 中标记为 generated。

## 0.9.1 · Chrome 153 连接诊断与安全回退

- 确认 Chrome 153 权限代理的 9222 返回 404 属于预期行为；`auto_connect` 仍依赖可读的 `DevToolsActivePort`。
- 默认改为持久专用 Chrome Profile，避开 Codex/macOS 沙箱对默认 Profile 端点文件的限制。
- 区分远程调试未开启、等待 Allow、ActivePort 缺失/无权限/格式错误、权限代理不兼容和真实 transport 断开。
- 连接错误后关闭并丢弃旧客户端与会话，确保下一次调用真正重连。
- 诊断只公开安全分类，不记录 Cookie、请求头、页面内容、表单值或 WebSocket token。
- 新增 Chrome 150+/153 故障矩阵、重连和持久专用 Profile 集成回归。

## 0.9.0 · 单一 DevTools 路线

- 从公开源码、安装包和运行时中移除旧 Chrome 扩展桥及 WebSocket 回退驱动。
- 本机保留扩展桥源码归档；公开 Git 历史可通过 `v0.8.0` 查看。
- 根项目收敛为 MCP 构建、虚构表单和发布脚本，减少前端构建依赖。
- README 新增可复制给本地 AI Agent 的自动安装与验证提示词。
- 首次连接说明与 Chrome 官方行为对齐：用户启用远程调试，并为新调试会话点击 Allow。

## 0.8.0 · Chrome DevTools 主驱动

- 默认通过固定版本的官方 Chrome DevTools MCP 连接当前 Chrome，保留现有招聘网站登录状态。
- 浏览器扩展降为可选兼容回退，日常安装不再依赖扩展或本地 WebSocket 桥。
- MCP 核心、资料库、快照、安全策略、操作日志和驱动改为严格 TypeScript。
- 浏览器层只开放标签页、无障碍快照、填写、点击、按键和纵向滚动所需的内部工具。
- 新增当前值校验、操作去重、条件撤销、保存边界和 Chrome 授权状态。
- 发布包内置固定的 Chrome DevTools 运行时，不依赖全局 npx 或运行时下载。
- 新增无扩展的 MCP 到隔离 Chrome 集成测试；继续保留扩展回退回归。
- 明确不需要 macOS 的“修改 App”权限，首次连接只接受 Chrome 自己的 Allow 授权。

## 0.7.1 · MCP 启动保护

- 将插件内置 MCP 标记为必需服务，任务加载时等待它完成初始化。
- 将启动超时提高到 30 秒，避免慢机器在初始工具目录中遗漏工具。
- skill 在保存资料或填写前先调用 resume_status，并区分 MCP 、本地资料库与 Chrome 桥接状态。
- 工具完全缺席时执行只读诊断，不会启动一个无法注册到当前任务的独立服务进程。

## 0.7.0 · MCP 本地资料库重构

- 将多份简历、原始 Markdown 和历史修订迁移到 MCP 本地资料库设计。
- 新增 resume_profile_list、resume_profile_read 和 resume_profile_save。
- 资料更新使用 expected_revision，拒绝并发会话静默覆盖。
- 资料引用在 MCP 中解析，Chrome 扩展只接收本次操作所需的字面值。
- 扩展删除简历编辑、Markdown 导入、模型 API、侧边栏、悬浮球和投递记录界面，只保留桥接设置与状态。
- 内容脚本改为按需注入，不再常驻所有网页。
- 升级时保留旧 resume_state，不自动清除用户原数据。
- 删除旧业务后端契约和已退出产品路线的测试。
- 更新通用 MCP 安装、Codex skill、安全说明和虚构表单回归。

## 0.6.0 · 开发预览版

- 新增由 Codex 组合的基础页面观察和动作工具。
- 覆盖动态候选、级联、年月弹层、虚拟滚动和重复经历。
- 最终申请提交由用户操作。
