{
  "name": "resume-companion-codex-plugin",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run typecheck && npm run test:unit",
    "test:unit": "vitest run --exclude tests/devtools-live.test.ts",
    "test:devtools": "vitest run tests/devtools-live.test.ts",
    "test:core": "node scripts/core-self-test.mjs",
    "check": "npm test && npm run build && npm run test:core && npm run test:devtools"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "chrome-devtools-mcp": "1.9.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
