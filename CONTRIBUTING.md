# 参与贡献

当前重点是 Codex 通过基础 MCP 工具连续完成复杂表单。欢迎改进通用控件、观察语义、可靠回读、使用体验与文档。

## 开发

请先阅读 [开发指南](docs/development.md)，安装根项目和 MCP 两套依赖。提交前运行 `npm run check`，修改 MCP 时同时运行构建、`npm test`、`npm run test:core` 和相应的实际扩展联调。

- 对行为变化补充有意义的回归，优先使用虚构招聘页面复现。
- 新控件先扩充观察和基础动作，避免给每个招聘站堆叠整套业务脚本。
- 保留当前值校验、操作去重和未知结果回读。保存记录/草稿及普通下一步在授权任务内允许；最终申请提交、上传、验证码、密码和声明仍由用户处理。
- 不在工具内部调用另一模型决定下一步，不增加项目自建的简历业务后端。
- 修改共享协议时，同时验证 MCP 的 Zod 3 和扩展的 Zod 4 行为。
- `server.bundle.mjs` 需要由源码重新构建；不要手工修改生成内容。

## Issue 与 PR

说明问题、触发条件、预期行为、扩展/浏览器版本及验证结果。仅使用合成资料，不提交真实简历、API Key、Cookie、网页会话、备份或招聘网页抓取副本。PR 描述围绕最终行为和测试证据。

安全问题请使用仓库的私密漏洞报告入口，见 [SECURITY.md](SECURITY.md)。
